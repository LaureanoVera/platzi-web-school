(() => {
  let prices: number[] = [10, 8, 71, 3, 59];
  prices.push(24);

  let mixed: (number | boolean)[] = [111, 1010, true, 10, 1101];
  mixed.push(false);

  let power: Object[] = [];
  power.push({ name: 'Laureano', age: 19, 'goku--?': true });

  let names: string[] = ['Laureano', 'Ivan', 'Gerardo'];
})();
